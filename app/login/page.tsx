'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link';
export default function Login(){const r=useRouter();const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});if(res.ok)r.push('/dashboard');else setError('بيانات الدخول غير صحيحة')}return <main className="hero"><form className="card form" onSubmit={submit}><h1>تسجيل الدخول</h1><input name="email" type="email" placeholder="البريد الإلكتروني" required/><input name="password" type="password" placeholder="كلمة المرور" required/>{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="btn">دخول</button><p className="muted">جديد؟ <Link href="/register">أنشئ حسابك</Link></p></form></main>}
