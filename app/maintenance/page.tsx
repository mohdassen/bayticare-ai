import { redirect } from 'next/navigation'; import { AppShell } from '@/components/AppShell'; import { getCurrentUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma';
export default async function Maintenance(){const u=await getCurrentUser();if(!u)redirect('/login');const rows=await prisma.maintenanceEvent.findMany({where:{asset:{property:{ownerId:u.id}}},include:{asset:true},orderBy:{dueAt:'asc'}});return <AppShell><h1>الصيانة</h1><div className="card list">{rows.length?rows.map(m=><div className="item" key={m.id}><div><strong>{m.title}</strong><div className="muted">{m.asset.name}</div></div><div><span className="badge">{m.completedAt?'مكتملة':m.dueAt<new Date()?'متأخرة':'قادمة'} · {m.dueAt.toLocaleDateString('ar-SA')}</span></div></div>):<p className="muted">أضف أصلًا وسيتم إنشاء خطة صيانة له.</p>}</div></AppShell>}
