'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link';
export default function Register(){const r=useRouter();const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});if(res.ok)r.push('/dashboard');else setError((await res.json()).error||'تعذر إنشاء الحساب')}return <main className="hero"><form className="card form" onSubmit={submit}><h1>إنشاء حساب</h1><label>الاسم</label><input name="name" required/><label>البريد الإلكتروني</label><input name="email" type="email" required/><label>الجوال</label><input name="phone" placeholder="+9665..."/><label>كلمة المرور</label><input name="password" type="password" minLength={8} required/>{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="btn">إنشاء الحساب</button><p className="muted">لديك حساب؟ <Link href="/login">تسجيل الدخول</Link></p></form></main>}
