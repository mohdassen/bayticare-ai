import { getCurrentUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { redirect } from 'next/navigation';
export async function POST(req:Request){const u=await getCurrentUser();if(!u)return new Response('Unauthorized',{status:401});const f=await req.formData();const p=await prisma.property.create({data:{ownerId:u.id,name:String(f.get('name')),type:String(f.get('type')) as any,ownership:'OWNED',city:String(f.get('city')),district:String(f.get('district')||'')}});await prisma.floor.create({data:{propertyId:p.id,name:'الدور الأرضي'}});redirect('/properties')}
