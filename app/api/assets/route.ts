import { getCurrentUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { redirect } from 'next/navigation';
export async function POST(req:Request){const u=await getCurrentUser();if(!u)return new Response('Unauthorized',{status:401});const f=await req.formData();const propertyId=String(f.get('propertyId'));const property=await prisma.property.findFirst({where:{id:propertyId,ownerId:u.id}});if(!property)return new Response('Forbidden',{status:403});const interval=Number(f.get('maintenanceIntervalDays')||180);const next=new Date();next.setDate(next.getDate()+interval);const asset=await prisma.asset.create({data:{propertyId,name:String(f.get('name')),category:String(f.get('category')),manufacturer:String(f.get('manufacturer')||''),model:String(f.get('model')||''),maintenanceIntervalDays:interval,nextMaintenanceAt:next}});await prisma.maintenanceEvent.create({data:{assetId:asset.id,userId:u.id,title:`صيانة ${asset.name}`,dueAt:next,status:'SCHEDULED'}});redirect('/assets')}
